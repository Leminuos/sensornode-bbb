#
# SensorNode development distro
#

require conf/distro/include/sensornode-base.inc

DISTRO = "sensornode-dev"

SENSORNODE_KEYDIR = "${SENSORNODE_DEV_KEYDIR}"

DISTRO_FEATURES:append = " \
    sensornode-ota \
    sensornode-hmi \
    sensornode-devtools \
    sensornode-login \
    sensornode-debug \
"
